<UserControl x:Class="Bau.Controls.Files.PathSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid x:Name="grdPathSelect">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<TextBox x:Name="txtPath" Grid.Column="0" Text="{Binding Path=PathName, Mode=TwoWay}" />
		<Button Content="..." Grid.Column="1" x:Name="cmdSelect" Click="cmdSelect_Click" />
	</Grid>
</UserControl>
